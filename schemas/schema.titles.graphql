# import Publisher from "schema.publishers.graphql"
# import Series from "schema.series.graphql"

type Title {
  id: ID!
  name: String!
  publisher_id: Int!
  url: String
  issues: Int
  year: Int
  volume: Int
  publisher: Publisher!
  date_created: Int!
  date_updated: Int!
  series: Series
  issue_count: Int
}
