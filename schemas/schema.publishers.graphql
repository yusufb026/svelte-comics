# import Title from "schema.titles.graphql"
# import Series from "schema.series.graphql"

type Publisher {
  id: ID!
  name: String!
  url: String
  titles: [Title]
  date_created: Int!
  date_updated: Int!
  series: Series
  title_count: Int
}
